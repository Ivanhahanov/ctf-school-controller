apiVersion: lab.ctf.school/v1
kind: LabSpace
metadata:
  labels:
    app.kubernetes.io/name: controller
    app.kubernetes.io/managed-by: kustomize
  name: labspace-sample
spec:
  # TODO(user): Add fields here
