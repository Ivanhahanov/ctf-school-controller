---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: labservices.infra.ctf.school
spec:
  group: infra.ctf.school
  names:
    kind: LabService
    listKind: LabServiceList
    plural: labservices
    singular: labservice
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: |-
          LabService is the Schema for the labservices API.
          It is Cluster-scoped because it serves as a global infrastructure definition.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the configuration of the service blueprint.
            properties:
              args:
                description: Args are the arguments passed to the command.
                items:
                  type: string
                type: array
              command:
                description: Command overrides the container entrypoint.
                items:
                  type: string
                type: array
              egress:
                description: |-
                  Egress is a per-service allow-list of external destinations this service may
                  reach. It is additive on top of the LabSpace network policy and is scoped to
                  this service's pods only: when empty the service gets no outbound internet
                  access. Use it to grant a single API (e.g. DeepSeek) instead of opening the
                  whole internet via LabSpace.Network.AllowInternet. Requires the Cilium CNI.
                items:
                  description: |-
                    EgressRule allow-lists outbound traffic from a single LabService to a specific
                    external destination. Rules are additive and scoped to this service's pods only,
                    so one challenge can be granted a single API without opening the whole internet.
                  properties:
                    cidrs:
                      description: CIDRs is a list of IP ranges the service may reach,
                        e.g. "203.0.113.0/24".
                      items:
                        type: string
                      type: array
                    fqdns:
                      description: |-
                        FQDNs is a list of DNS names the service may reach, e.g. "api.deepseek.com".
                        A leading "*." wildcard is supported ("*.googleapis.com"). Enforced via the
                        Cilium toFQDNs mechanism, which requires the Cilium CNI.
                      items:
                        type: string
                      type: array
                    ports:
                      description: Ports restricts this rule to specific destination
                        ports. Empty means all ports.
                      items:
                        description: EgressPort restricts an EgressRule to a specific
                          destination port/protocol.
                        properties:
                          port:
                            description: Port is the destination port number.
                            format: int32
                            maximum: 65535
                            minimum: 1
                            type: integer
                          protocol:
                            default: TCP
                            description: Protocol is TCP or UDP. Defaults to TCP.
                            enum:
                            - TCP
                            - UDP
                            type: string
                        required:
                        - port
                        type: object
                      type: array
                  type: object
                type: array
              env:
                description: Env is a list of environment variables to set in the
                  container.
                items:
                  description: EnvVar defines a custom environment variable, which
                    can be static or dynamically generated.
                  properties:
                    dynamicValue:
                      description: DynamicValue defines how to generate a unique value
                        for this variable per session.
                      properties:
                        length:
                          description: Length specifies the character count for "random"
                            type generation.
                          type: integer
                        template:
                          description: Template is a format string for "hmac" or "random"
                            results (e.g., "CTF{%s}").
                          type: string
                        type:
                          description: 'Type defines the generation strategy. Supported:
                            "random", "hmac".'
                          enum:
                          - random
                          - hmac
                          type: string
                      required:
                      - type
                      type: object
                    name:
                      description: Name of the environment variable. Must be a C_IDENTIFIER.
                      type: string
                    value:
                      description: Value is a static string value.
                      type: string
                  required:
                  - name
                  type: object
                type: array
              exposure:
                description: Exposure defines how this service should be reached from
                  the outside world.
                properties:
                  targetPort:
                    description: TargetPort is the name (preferred) or number of the
                      port in the container to expose.
                    type: string
                  type:
                    description: Type defines the protocol and UI handling (HTTP,
                      GRPC, Terminal, VNC).
                    enum:
                    - HTTP
                    - GRPC
                    - Terminal
                    - VNC
                    type: string
                required:
                - targetPort
                - type
                type: object
              image:
                description: Image is the Docker image repository and tag.
                type: string
              imagePullPolicy:
                default: IfNotPresent
                description: ImagePullPolicy defines when to pull the image (Always,
                  IfNotPresent, Never).
                type: string
              liveness:
                description: LivenessProbe describes when to restart the container
                  using standard K8s probe logic.
                properties:
                  exec:
                    description: Exec specifies a command to execute in the container.
                    properties:
                      command:
                        description: |-
                          Command is the command line to execute inside the container, the working directory for the
                          command  is root ('/') in the container's filesystem. The command is simply exec'd, it is
                          not run inside a shell, so traditional shell instructions ('|', etc) won't work. To use
                          a shell, you need to explicitly call out to that shell.
                          Exit status of 0 is treated as live/healthy and non-zero is unhealthy.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: atomic
                    type: object
                  failureThreshold:
                    description: |-
                      Minimum consecutive failures for the probe to be considered failed after having succeeded.
                      Defaults to 3. Minimum value is 1.
                    format: int32
                    type: integer
                  grpc:
                    description: GRPC specifies a GRPC HealthCheckRequest.
                    properties:
                      port:
                        description: Port number of the gRPC service. Number must
                          be in the range 1 to 65535.
                        format: int32
                        type: integer
                      service:
                        default: ""
                        description: |-
                          Service is the name of the service to place in the gRPC HealthCheckRequest
                          (see https://github.com/grpc/grpc/blob/master/doc/health-checking.md).

                          If this is not specified, the default behavior is defined by gRPC.
                        type: string
                    required:
                    - port
                    type: object
                  httpGet:
                    description: HTTPGet specifies an HTTP GET request to perform.
                    properties:
                      host:
                        description: |-
                          Host name to connect to, defaults to the pod IP. You probably want to set
                          "Host" in httpHeaders instead.
                        type: string
                      httpHeaders:
                        description: Custom headers to set in the request. HTTP allows
                          repeated headers.
                        items:
                          description: HTTPHeader describes a custom header to be
                            used in HTTP probes
                          properties:
                            name:
                              description: |-
                                The header field name.
                                This will be canonicalized upon output, so case-variant names will be understood as the same header.
                              type: string
                            value:
                              description: The header field value
                              type: string
                          required:
                          - name
                          - value
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      path:
                        description: Path to access on the HTTP server.
                        type: string
                      port:
                        anyOf:
                        - type: integer
                        - type: string
                        description: |-
                          Name or number of the port to access on the container.
                          Number must be in the range 1 to 65535.
                          Name must be an IANA_SVC_NAME.
                        x-kubernetes-int-or-string: true
                      scheme:
                        description: |-
                          Scheme to use for connecting to the host.
                          Defaults to HTTP.
                        type: string
                    required:
                    - port
                    type: object
                  initialDelaySeconds:
                    description: |-
                      Number of seconds after the container has started before liveness probes are initiated.
                      More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes
                    format: int32
                    type: integer
                  periodSeconds:
                    description: |-
                      How often (in seconds) to perform the probe.
                      Default to 10 seconds. Minimum value is 1.
                    format: int32
                    type: integer
                  successThreshold:
                    description: |-
                      Minimum consecutive successes for the probe to be considered successful after having failed.
                      Defaults to 1. Must be 1 for liveness and startup. Minimum value is 1.
                    format: int32
                    type: integer
                  tcpSocket:
                    description: TCPSocket specifies a connection to a TCP port.
                    properties:
                      host:
                        description: 'Optional: Host name to connect to, defaults
                          to the pod IP.'
                        type: string
                      port:
                        anyOf:
                        - type: integer
                        - type: string
                        description: |-
                          Number or name of the port to access on the container.
                          Number must be in the range 1 to 65535.
                          Name must be an IANA_SVC_NAME.
                        x-kubernetes-int-or-string: true
                    required:
                    - port
                    type: object
                  terminationGracePeriodSeconds:
                    description: |-
                      Optional duration in seconds the pod needs to terminate gracefully upon probe failure.
                      The grace period is the duration in seconds after the processes running in the pod are sent
                      a termination signal and the time when the processes are forcibly halted with a kill signal.
                      Set this value longer than the expected cleanup time for your process.
                      If this value is nil, the pod's terminationGracePeriodSeconds will be used. Otherwise, this
                      value overrides the value provided by the pod spec.
                      Value must be non-negative integer. The value zero indicates stop immediately via
                      the kill signal (no opportunity to shut down).
                      This is a beta field and requires enabling ProbeTerminationGracePeriod feature gate.
                      Minimum value is 1. spec.terminationGracePeriodSeconds is used if unset.
                    format: int64
                    type: integer
                  timeoutSeconds:
                    description: |-
                      Number of seconds after which the probe times out.
                      Defaults to 1 second. Minimum value is 1.
                      More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes
                    format: int32
                    type: integer
                type: object
              ports:
                description: Ports defines the list of network ports to be opened
                  in the container.
                items:
                  description: ContainerPort represents a network port in a single
                    container.
                  properties:
                    containerPort:
                      description: |-
                        Number of port to expose on the pod's IP address.
                        This must be a valid port number, 0 < x < 65536.
                      format: int32
                      type: integer
                    hostIP:
                      description: What host IP to bind the external port to.
                      type: string
                    hostPort:
                      description: |-
                        Number of port to expose on the host.
                        If specified, this must be a valid port number, 0 < x < 65536.
                        If HostNetwork is specified, this must match ContainerPort.
                        Most containers do not need this.
                      format: int32
                      type: integer
                    name:
                      description: |-
                        If specified, this must be an IANA_SVC_NAME and unique within the pod. Each
                        named port in a pod must have a unique name. Name for the port that can be
                        referred to by services.
                      type: string
                    protocol:
                      default: TCP
                      description: |-
                        Protocol for port. Must be UDP, TCP, or SCTP.
                        Defaults to "TCP".
                      type: string
                  required:
                  - containerPort
                  type: object
                type: array
              readiness:
                description: ReadinessProbe describes when the container is ready
                  to accept traffic.
                properties:
                  exec:
                    description: Exec specifies a command to execute in the container.
                    properties:
                      command:
                        description: |-
                          Command is the command line to execute inside the container, the working directory for the
                          command  is root ('/') in the container's filesystem. The command is simply exec'd, it is
                          not run inside a shell, so traditional shell instructions ('|', etc) won't work. To use
                          a shell, you need to explicitly call out to that shell.
                          Exit status of 0 is treated as live/healthy and non-zero is unhealthy.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: atomic
                    type: object
                  failureThreshold:
                    description: |-
                      Minimum consecutive failures for the probe to be considered failed after having succeeded.
                      Defaults to 3. Minimum value is 1.
                    format: int32
                    type: integer
                  grpc:
                    description: GRPC specifies a GRPC HealthCheckRequest.
                    properties:
                      port:
                        description: Port number of the gRPC service. Number must
                          be in the range 1 to 65535.
                        format: int32
                        type: integer
                      service:
                        default: ""
                        description: |-
                          Service is the name of the service to place in the gRPC HealthCheckRequest
                          (see https://github.com/grpc/grpc/blob/master/doc/health-checking.md).

                          If this is not specified, the default behavior is defined by gRPC.
                        type: string
                    required:
                    - port
                    type: object
                  httpGet:
                    description: HTTPGet specifies an HTTP GET request to perform.
                    properties:
                      host:
                        description: |-
                          Host name to connect to, defaults to the pod IP. You probably want to set
                          "Host" in httpHeaders instead.
                        type: string
                      httpHeaders:
                        description: Custom headers to set in the request. HTTP allows
                          repeated headers.
                        items:
                          description: HTTPHeader describes a custom header to be
                            used in HTTP probes
                          properties:
                            name:
                              description: |-
                                The header field name.
                                This will be canonicalized upon output, so case-variant names will be understood as the same header.
                              type: string
                            value:
                              description: The header field value
                              type: string
                          required:
                          - name
                          - value
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      path:
                        description: Path to access on the HTTP server.
                        type: string
                      port:
                        anyOf:
                        - type: integer
                        - type: string
                        description: |-
                          Name or number of the port to access on the container.
                          Number must be in the range 1 to 65535.
                          Name must be an IANA_SVC_NAME.
                        x-kubernetes-int-or-string: true
                      scheme:
                        description: |-
                          Scheme to use for connecting to the host.
                          Defaults to HTTP.
                        type: string
                    required:
                    - port
                    type: object
                  initialDelaySeconds:
                    description: |-
                      Number of seconds after the container has started before liveness probes are initiated.
                      More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes
                    format: int32
                    type: integer
                  periodSeconds:
                    description: |-
                      How often (in seconds) to perform the probe.
                      Default to 10 seconds. Minimum value is 1.
                    format: int32
                    type: integer
                  successThreshold:
                    description: |-
                      Minimum consecutive successes for the probe to be considered successful after having failed.
                      Defaults to 1. Must be 1 for liveness and startup. Minimum value is 1.
                    format: int32
                    type: integer
                  tcpSocket:
                    description: TCPSocket specifies a connection to a TCP port.
                    properties:
                      host:
                        description: 'Optional: Host name to connect to, defaults
                          to the pod IP.'
                        type: string
                      port:
                        anyOf:
                        - type: integer
                        - type: string
                        description: |-
                          Number or name of the port to access on the container.
                          Number must be in the range 1 to 65535.
                          Name must be an IANA_SVC_NAME.
                        x-kubernetes-int-or-string: true
                    required:
                    - port
                    type: object
                  terminationGracePeriodSeconds:
                    description: |-
                      Optional duration in seconds the pod needs to terminate gracefully upon probe failure.
                      The grace period is the duration in seconds after the processes running in the pod are sent
                      a termination signal and the time when the processes are forcibly halted with a kill signal.
                      Set this value longer than the expected cleanup time for your process.
                      If this value is nil, the pod's terminationGracePeriodSeconds will be used. Otherwise, this
                      value overrides the value provided by the pod spec.
                      Value must be non-negative integer. The value zero indicates stop immediately via
                      the kill signal (no opportunity to shut down).
                      This is a beta field and requires enabling ProbeTerminationGracePeriod feature gate.
                      Minimum value is 1. spec.terminationGracePeriodSeconds is used if unset.
                    format: int64
                    type: integer
                  timeoutSeconds:
                    description: |-
                      Number of seconds after which the probe times out.
                      Defaults to 1 second. Minimum value is 1.
                      More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes
                    format: int32
                    type: integer
                type: object
              resources:
                description: |-
                  Resources defines CPU/memory/ephemeral-storage constraints for this service.
                  When no ephemeral-storage LIMIT is given the controller injects a conservative
                  default so a challenge cannot fill node disk via its writable rootfs; set it
                  explicitly here to raise or lower that cap for disk-heavy challenges.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              security:
                description: |-
                  Security relaxes the platform's secure-by-default pod hardening for THIS service.
                  When absent the container is fully locked down (see SecurityProfile). Relax only
                  what the workload genuinely needs — every field defaults to the secure setting.
                properties:
                  addCapabilities:
                    description: |-
                      AddCapabilities are Linux capabilities to add back on top of the dropped set.
                      Keep minimal (e.g. NET_BIND_SERVICE). Default: none.
                    items:
                      description: Capability represent POSIX capabilities type
                      type: string
                    type: array
                  allowPrivilegeEscalation:
                    description: |-
                      AllowPrivilegeEscalation permits gaining more privileges than the parent process
                      (setuid binaries / file capabilities). Default false (no-new-privs).
                    type: boolean
                  allowRunAsRoot:
                    description: AllowRunAsRoot lets the container run as UID 0 (disables
                      runAsNonRoot).
                    type: boolean
                  keepDefaultCapabilities:
                    description: |-
                      KeepDefaultCapabilities skips the "drop ALL capabilities" default and keeps the
                      container runtime's default set. Coarse — prefer AddCapabilities. Default false.
                    type: boolean
                  privileged:
                    description: |-
                      Privileged runs a fully privileged container — near-host access. DANGEROUS and
                      blocked by the namespace Pod Security 'baseline' enforcement; use only on a
                      cluster that permits it. Default false.
                    type: boolean
                  runAsUser:
                    description: RunAsUser pins a specific UID. When unset the strict
                      default is 1000.
                    format: int64
                    type: integer
                  seccompUnconfined:
                    description: SeccompUnconfined disables the RuntimeDefault seccomp
                      profile. Default false.
                    type: boolean
                  writableRootFilesystem:
                    description: |-
                      WritableRootFilesystem disables the read-only root filesystem. Prefer mounting an
                      emptyDir on the specific writable path instead. Default false (root fs read-only).
                    type: boolean
                type: object
              storageLimit:
                anyOf:
                - type: integer
                - type: string
                description: |-
                  StorageLimit caps the size of the container's writable /tmp scratch volume (an
                  emptyDir mounted when the root filesystem stays read-only). Defaults to 256Mi.
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
            required:
            - image
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
