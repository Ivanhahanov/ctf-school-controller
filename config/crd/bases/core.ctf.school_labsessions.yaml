---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: labsessions.core.ctf.school
spec:
  group: core.ctf.school
  names:
    kind: LabSession
    listKind: LabSessionList
    plural: labsessions
    singular: labsession
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.userId
      name: User
      type: string
    - jsonPath: .spec.labSpaceRef
      name: Lab
      type: string
    - jsonPath: .status.expiredTime
      name: Expires
      type: string
    - jsonPath: .status.message
      name: Message
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: LabSession is the Schema for the labsessions API, representing
          an active lab instance.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the configuration of the session.
            properties:
              labSpaceRef:
                description: LabSpaceRef is the name of the Cluster-scoped LabSpace
                  template to instantiate.
                type: string
              userId:
                description: UserId is the unique identifier of the student/user who
                  owns this session.
                type: string
            required:
            - labSpaceRef
            - userId
            type: object
          status:
            description: Status defines the current state of the session.
            properties:
              conditions:
                description: |-
                  Conditions represent the detailed state transitions of the session.
                  Common types: InfrastructureReady, ServicesHealthy, Ready.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              endpoints:
                description: Endpoints lists all public-facing addresses for the lab
                  services.
                items:
                  description: Endpoint represents a network entry point for the user
                    to interact with the lab.
                  properties:
                    address:
                      description: Address is the fully qualified domain name or IP
                        with optional port/protocol.
                      type: string
                    serviceName:
                      description: ServiceName refers to the name of the LabService
                        within the session.
                      type: string
                    type:
                      description: Type defines the protocol/access method (e.g.,
                        HTTP, Terminal, GRPC, VNC).
                      enum:
                      - HTTP
                      - Terminal
                      - GRPC
                      - VNC
                      type: string
                  required:
                  - address
                  - serviceName
                  - type
                  type: object
                type: array
              expiredTime:
                description: ExpiredTime is the calculated or actual moment when the
                  session is/was terminated.
                format: date-time
                type: string
              message:
                description: Message
                type: string
              phase:
                default: Pending
                description: Phase is a high-level summary of the session lifecycle.
                enum:
                - Pending
                - Running
                - Failed
                - Terminating
                - Expired
                type: string
              startTime:
                description: StartTime is the moment the lab infrastructure became
                  Ready.
                format: date-time
                type: string
              summary:
                description: Summary
                properties:
                  activeServices:
                    type: integer
                  namespace:
                    type: string
                  totalServices:
                    type: integer
                required:
                - activeServices
                - namespace
                - totalServices
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
